name: One-shot Interior Non-product Patch

on:
  push:
    paths:
      - '.github/workflows/patch-interior-nonproduct-now.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: python patch_interior_nonproduct.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add queue.json
          git commit -m "Polish interior engagement and room idea posts"
          git pull --rebase origin main
          git push origin HEAD:main
